import { EmptyState } from "@/shared/ui/empty-state";

/**
 * EmptyLayout — оформленное пустое состояние публичной страницы номинации,
 * пока раскладка черновая (спека 0035, FR-23/AC-6): показывается вместо
 * карточек групп/сетки, когда ни один пул и ни одна сетка ещё не
 * зафиксированы. Шапку и цепочку схемы (если этапы уже заведены) решает
 * показывать композиция экрана — этот компонент отвечает только за сам
 * блок пустого состояния (0022/0023, без «Загрузка…» и спиннеров).
 */
export function EmptyLayout() {
  return (
    <EmptyState
      title="Раскладка ещё формируется"
      hint="Загляните позже — группы и сетка появятся здесь, как только оргкомитет зафиксирует раскладку."
    />
  );
}
