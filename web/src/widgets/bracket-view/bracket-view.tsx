// Compatibility entrypoint for public stage compositions. The graph itself
// belongs to the bracket entity and is also used directly by admin seeding.
export { BracketGraph as BracketView } from "@/entities/bracket/ui/bracket-graph";
