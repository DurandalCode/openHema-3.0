---
task: T<N>
feature: docs/specs/NNN-<kebab-feature>/
status: pending
requires: local
depends_on: []
---

# T<N> — <короткое название задачи, как в tasks.md>

> Артефакт ADR 0021. Самодостаточная карточка: исполнитель работает
> **только** с этим файлом и файлами, которые он явно называет — не
> заглядывает в `AGENTS.md`, `spec.md`, `plan.md` или другие задачи.

## Цель

<1-2 предложения — что должно получиться после выполнения задачи.>

## Контекст

<Инлайненные куски существующего кода, на которые нужно ориентироваться:
сигнатуры соседних портов/интерфейсов, пример аналогичной реализации из
другого модуля, текущее содержимое затрагиваемого файла (если он уже
существует). Не "почитай файл X" — сам релевантный код, вставленный сюда.>

## Red — тест первым

Путь: `<path/to/xxx_test.go | *.test.ts>`

<Точный код теста, либо — если это невозможно дословно — максимально явный
псевдокод: входные данные, вызов, ожидаемый результат/ошибка.>

## Green — минимальная реализация

Путь: `<path/to/xxx.go | *.ts>`

<Что именно написать, чтобы тест из Red стал зелёным. Минимально
достаточное — без вещей, не нужных для прохождения теста.>

## Acceptance

Команда: `<напр. go test ./modules/x/... | pnpm test path/to/file.test.ts>`

Критерий: <что именно должно быть true — тест зелёный, других тестов не
сломано, и т.п.>

## Boundaries

- Не создавать файлы сверх перечисленных в Red/Green.
- Не трогать: `<файлы/директории, которые эта задача не должна касаться>`.
- Если что-то в карточке противоречит текущему коду или неполно —
  остановиться и сообщить, что именно неясно; не импровизировать.
