# Tasks: admin-mobile-baseline

> Артефакт SDD (ADR 0008) + TDD-чеклист (ADR 0009). Упорядоченный список шагов.
> Каждая задача = слой/файл + пара «тест → код» по циклу red → green → refactor.

- Статус: done
- Дата: 2026-08-30
- План: `./plan.md`

## Порядок

Задачи выполняются сверху вниз. Внутри задачи: сначала падающий тест (red),
затем минимальный код (green), затем рефактор при зелёных тестах.

## Треки и параллельность

Волна 0 — общие примитивы каркаса (`shared/ui`, `widgets/admin-shell`),
от которых зависят все экраны; выполняется последовательно (файлы малы и
частично соседствуют, дробить на треки не даёт выигрыша). Волна 1 —
применение примитивов на конкретных экранах админки: у каждого экрана свои
файлы (`features/*/ui/*`, `app/(admin)/admin/**/page.tsx`), треки не
пересекаются и не ждут друг друга — идут параллельно. Волна 2 — join:
полный прогон тестов, сборка, ручная проверка на нескольких ширинах,
обновление статусов.

| Волна | Трек | Задачи   | Файлы (не пересекаются внутри волны)                          | Зависит от |
| ----- | ---- | -------- | -------------------------------------------------------------- | ---------- |
| 0     | —    | T1-T5    | `shared/ui/*`, `widgets/admin-shell/*`                          | —          |
| 1     | A    | T6       | `features/fighter-management/ui/fighters-table.tsx`, `app/(admin)/admin/fighters/page.tsx` | волна 0 |
| 1     | B    | T7       | арены: список + `app/(admin)/admin/arenas/[id]/page.tsx` (живой скоринг) | волна 0 |
| 1     | C    | T8       | заявки: `app/(admin)/admin/applications/page.tsx` и её виджет   | волна 0 |
| 1     | D    | T9       | номинации: `app/(admin)/admin/nominations/**`                   | волна 0 |
| 1     | E    | T10      | форматы + пульт: `app/(admin)/admin/formats/page.tsx`, `app/(admin)/admin/console/page.tsx` | волна 0 |
| 1     | F    | T10a     | пользователи: `app/(admin)/admin/page.tsx`, `features/admin/ui/users-table.tsx` | волна 0 |
| 2     | join | T11-T15  | (весь репозиторий — проверка, без новых правок логики)          | треки A-F смержены |

## Инвентаризация

- [x] T1. Пройтись по всем экранам `app/(admin)/admin/**` и их
      `features/*/ui`/`widgets/*` компонентам — сделано разведкой при
      планировании (2026-08-30), находки:
      - Реальные (не empty-state) десктопные `max-w-*`-обёртки на основном
        контенте: `arenas/[id]/page.tsx` (`max-w-6xl` вокруг `ArenaConsole`
        — живой скоринг, T7) и `nominations/[id]/stages/page.tsx`
        (`max-w-6xl` вокруг основного контента этапов, T9). Остальные
        `max-w-*` в `app/(admin)/admin/**` — либо на empty-state карточках
        («нет активного турнира», не трогаем), либо на параграфе текста
        (`preset-library.tsx`, читаемая ширина абзаца — не баг).
      - Таблицы без горизонтального скролл-контейнера (`TableHead` +
        строки, требуют `table-scroll`, T3): `fighters-table.tsx` (T6),
        `arenas-table.tsx` (T7), `applications-table.tsx` (T8),
        `nominations-table.tsx` (T9), `users-table.tsx` (T10a — новый трек
        F, экран `/admin` «Пользователи» не был учтён в исходном плане).
      - `formats`/`console` (T10): списки не табличные (карточки/кастомные
        виджеты) — `TableHead` не используют, `max-w-*` на реальном
        контенте нет; проверка ограничивается FR-5 (тач-таргеты) и, для
        пульта, FR-7/AC-5 (отсутствие горизонтального оверфлоу страницы).

## Общие примитивы (волна 0)

- [x] T2. **dialog full-screen (red→green)** —
      `shared/ui/dialog.test.tsx`: `DialogContent` получает full-screen
      классы (`inset-0`, `h-full`, `w-full`, без `translate`/`top-[50%]`) на
      узком брейкпоинте и сохраняет текущие центрированные классы от `sm:` →
      затем правка `shared/ui/dialog.tsx`. Прогнать полный `pnpm test` —
      регрессия по всем существующим потребителям `Dialog` (confirm-dialog,
      auth-dialog, admin-модалки создания/редактирования и т.д.) не
      допускается.
- [x] T3. **table-scroll (red→green)** — `shared/ui/table-scroll.test.tsx`:
      компонент рендерит `children`, контейнер содержит класс
      `overflow-x-auto` → затем `shared/ui/table-scroll.tsx`.
- [x] T4. **admin-nav-drawer (red→green)** —
      `widgets/admin-shell/admin-nav-drawer.test.tsx`: рендерит все пункты
      `ADMIN_NAV_ITEMS` (тот же источник, что у `admin-nav-links.tsx`),
      триггер открывает панель, выбор пункта и Esc закрывают, активный
      пункт помечен `aria-current` → затем
      `widgets/admin-shell/admin-nav-drawer.tsx` (поверх Radix `Dialog`,
      side-slide классами, без новой зависимости — см. `plan.md`, «Риски»).
- [x] T5. **admin-shell wiring (red→green)** —
      `widgets/admin-shell/admin-shell.test.tsx`: расширить — в дереве
      присутствуют оба варианта навигации, `AdminNavLinks` под классом
      `hidden md:flex`, `AdminNavDrawer` под `md:hidden` → затем правка
      `admin-nav-links.tsx` (обёртка видимости) и `admin-shell.tsx`
      (рендерит оба вместо одного `nav={<AdminNavLinks />}`).

## Применение на экранах (волна 1, треки параллельны)

- [x] T6. **Трек A — Бойцы**: обернуть таблицу ростера в `table-scroll`
      (`fighters-table.tsx`), снять `max-w-*` с основного контента
      `fighters/page.tsx`/`fighters-screen.tsx`, если он там есть (T1).
      Тест: существующие `fighters-table.test.tsx`/`fighters-screen.test.tsx`
      остаются зелёными + при необходимости новая проверка обёртки.
- [x] T7. **Трек B — Площадки + живой скоринг**: `table-scroll` вокруг
      `arenas-table.tsx`; снять `max-w-6xl` с `arenas/[id]/page.tsx` (T1) —
      живой скоринг (`ArenaConsole`), FR-7/AC-5, точечные вёрсточные правки
      внутри `widgets/arena-console/*` (flex-wrap, отказ от жёстких ширин
      > 360px) без изменения состава/логики. Тест: существующие тесты
      экрана/виджетов остаются зелёными.
- [x] T8. **Трек C — Заявки**: `table-scroll` вокруг `applications-table.tsx`.
      Реальных `max-w-*` на основном контенте `applications/page.tsx` нет
      (T1) — только на empty-state, не трогаем. Тест: существующие тесты
      экрана/виджета остаются зелёными.
- [x] T9. **Трек D — Номинации**: `table-scroll` вокруг
      `nominations-table.tsx`; снять `max-w-6xl` с основного контента
      `nominations/[id]/stages/page.tsx` (T1, по прецеденту уже сделанного
      для `stages/[stageId]/page.tsx`). Тест: существующие тесты остаются
      зелёными.
- [x] T10. **Трек E — Форматы + Пульт**: по находкам T1 — табличных
      элементов нет ни там, ни там; проверка/точечная правка тач-таргетов
      (FR-5) и, для `console` (пульт), FR-7/AC-5 (без горизонтального
      оверфлоу страницы) в `features/tournament-console/ui/*`. Тест:
      существующие тесты остаются зелёными.
- [x] T10a. **Трек F — Пользователи**: `table-scroll` вокруг
      `users-table.tsx` (экран `/admin`, найден в T1 — не был учтён в
      исходном `plan.md`). Тест: существующие `users-table.test.tsx`/
      `users-screen.test.tsx` остаются зелёными.

## Проверка (волна 2, join)

- [x] T11. `make test-web` (или `pnpm test`) зелёный по всему `web/`.
- [x] T12. `pnpm exec tsc --noEmit`.
- [x] T13. `pnpm build`.
- [x] T14. Ручная проверка в браузере на 360px и 1280px (768px отдельно не
      гонял — поведение брейкпоинтов бинарное `md:`, между 360 и 1280
      ничего не меняется) через изолированный Playwright-стенд (временные
      postgres-контейнер/go-сервер/next dev на отдельном порту, локальная
      БД пользователя не тронута): вход bootstrap-админом, создание
      тестового турнира/площадки. Проверено: `/admin` (Пользователи) —
      навигация через `AdminNavDrawer` (все 8 пунктов, AC-4), таблица
      пользователей скроллится в своём контейнере, а не страница целиком
      (700px контента в ~326px видимой области, AC-3/FR-8), модалка
      создания админа full-screen на 360px и центрирована на 1280px
      (AC-6); `/admin/console` (пульт) и `/admin/arenas/[id]` (живой
      скоринг, с реальной площадкой) — без горизонтального скролла
      страницы на 360px (AC-5). На 1280px — навигация в один ряд (мобильный
      триггер скрыт), десктопное поведение не сломано. Найден и исправлен
      по ходу проверки один реальный баг вне исходной инвентаризации T1:
      `PageHeader` (общий для всех экранов) — см. отдельный коммит.
      Остальные экраны (арены/заявки/номинации/форматы списком, полный бой
      на арене) отдельно не прогонялись живьём — юнит-тесты треков A-F и
      сборка/типы дают достаточную уверенность, экраны используют те же
      общие примитивы (`table-scroll`, `Dialog`, `AdminNavDrawer`), что уже
      подтверждены визуально.
- [x] T15. Обновить статус спеки/плана/tasks на `done` и строку в
      `docs/specs/README.md`.
