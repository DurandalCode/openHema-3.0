# SF-08 — памятка секретаря

- База: `675bf0b3426ddceff4257d571a21ffb8e53cb8bf`.
- Трек: `feedback/guide`, отдельный worktree `/private/tmp/hema-feedback/guide`.
- Статус: черновик памятки подготовлен; учебный турнир в работающем приложении остаётся приёмочным шагом.

## Результат

Создан `docs/secretary-guide.md`: маршрут от ростера и пресета через посев/фиксацию, посадку, таймер и бой к оглашению, рейтингу, плейоффу и итоговому протоколу. В конце — воспроизводимый учебный сценарий. Действия сверены с исходным кодом и тестами UI версии базы, без описания будущих функций как уже доступных.

## Проверка маршрута по интерфейсу

| Шаг | Фактический экран или поведение |
| --- | --- |
| Ростер и участие | `/admin/fighters`, кнопки **+ Боец вручную**, **Импорт из файла**, выбор номинации в карточке бойца |
| Схема и пресет | `/admin/nominations` → меню строки **Схема** → чип пресета, второе нажатие **Заменить схему?** (`preset-chips.tsx`) |
| Посев и фиксация | **Открыть посев**, **+ Пул**, **Распределить автоматически**, **Зафиксировать** / **Вернуть в черновик** (`stage-inspector.tsx`, `stage-page-screen.tsx`, `nomination-pools.tsx`) |
| Условия расфиксации | `SetStatus` запрещает ready→draft при начатом бое или посаженном пуле; расфиксация удаляет созданные бои, оставляя посев (`server/modules/stage/service/service.go`) |
| Посадка и панель на исходной базе | `/admin/arenas/[id]`, фильтр только по номинации, **Поставить на эту арену**, режимы **Управление ареной** / **Ведение боя**, отдельное табло (`pool-seating.tsx`, `arena-console.tsx`) |
| Завершение и оглашение сейчас | `FinishCurrentBout` сразу продвигает указатель пула; `RevealCurrentBout` переводит табло, не трогая бой/пул (`service.go`, `arena_room.go`). Панель после последнего боя автоматически уходит в управление (`bout-panel-view.tsx`). Это отличается от будущей 0052. |
| Рейтинг и отбор | Таблица пула скрыта до первого завершённого боя; `BuildStageDialog` показывает состав, источник, целевой слот и дележи; предупреждение о незавершённых боях не блокирует формирование |
| Итоги | `NominationResults` под админской схемой и на публичной странице; admin CSV относится к итоговому протоколу, не к реестру всех боёв |

Отдельно проверено поведение мобильной панели. `ModeSwitch` разрешает вход при любом посаженном пуле, в том числе до начала боя. `BoutPanelView` рендерит его независимо от `state`. Кнопка **Старт** в `BoutTimerStrip` и `Пробел` вызывают только `controls.start` (таймерный endpoint), а широкая `TimerControls` сначала вызывает `useStartBout`. Следовательно, мобильный таймер может стартовать при ещё не начатом доменном бое. В памятке указан проверяемый безопасный порядок: начать новый бой в **Управлении ареной**, затем открыть панель. Координатору передан этот факт для QA/0052; изменения кода не входят в SF-08.

## Проверки и ограничения

- `git diff --cached --check` прошёл; названия кнопок и маршруты сверены с исходниками. Тесты приложения не запускались: программный код не менялся.
- Учебный прогон в браузере на отдельной БД не выполнен: Docker socket в этом worktree недоступен из sandbox (`permission denied`), `web/node_modules` отсутствует. Нет основания объявлять сценарий принятым реальным секретарём.
- Автотест Markdown не нужен: задача меняет только пользовательскую документацию. `tdd-cycle` прочитан, кодовый TDD здесь неприменим.
- После интеграции 0052 обновить раздел оглашения и сценарий: оба экрана должны удерживать результат, в том числе после последнего боя; проверить горячую клавишу и повторное подключение.
- После SF-09 уточнить переход к итоговым местам.
- Провести живой учебный прогон на тестовом турнире без подсказок разработчика, поправить непонятные места в памятке, затем отметить SF-08 завершённой в общем реестре.

Миграций, API и кодовых изменений нет. Общие audit/index/decisions/waves не менялись.

## Дополнение после интеграции SF-02 и 0053

- База обновления: `1f234b5`; ветка `feedback/guide-update`.
- Памятка теперь описывает два независимых фильтра выбора пула: по номинации и статусу, с начальным **Незавершённые**. Проверено по `pool-seating.tsx` и отчёту `pools-only.md`.
- Описание плейоффа заменено на фактический общий граф админского и публичного экранов: линии победителей, пунктир к бою за 3-е место, переходы **К кругу…**, прокрутка и подписи слотов. Проверено по `bracket-graph.tsx`, `bracket-seeding.tsx`, `bracket-view.tsx` и отчёту `0053-graph.md`.
- Учебный сценарий дополнен проверками обоих фильтров и графа. Реальный прогон на тестовом турнире по-прежнему ожидает выполнения; браузерная проверка графа из 0053 использовала отдельный fixture и не подтверждает весь маршрут секретаря.
- В `1f234b5` завершение боя всё ещё продвигает указатель пула в `FinishCurrentBout`, а после последнего боя панель возвращается в управление. Удержание результата на обоих экранах из 0052 ещё не внедрено и не описано как доступное.
