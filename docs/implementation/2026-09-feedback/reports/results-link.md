# SF-09 / M07 — переход к итоговым местам

- Base: `a1d67b0`; branch: `feedback/results-link`.
- Scope: только UI-композиция готового протокола, без нового расчёта мест, RPC и миграций.
- Админский экран этапа и публичная секция сетки показывают ссылку «Итоговые места» на `/nominations/[id]#results`, когда этап-сетка завершён и у него есть непустая законченная секция итогового протокола. Так завершённая промежуточная сетка без итоговых мест не ведёт к отсутствующему блоку.
- `NominationResults` даёт ровно один `id="results"` на странице, только если есть видимые секции. Цель фрагмента имеет `tabIndex=-1` и `scroll-mt-24` для клавиатурного фокуса и отступа от шапки.
- Протокол продолжает отображать 1/2/3/4 после бронзового боя и 3–4 для двух полуфиналистов без него; незавершённая секция не содержит итогового чемпиона.

## Проверки

- TDD red: 3 целевых теста падали без ссылки/якоря; затем green.
- Targeted Vitest: 4 файла, 37 тестов — pass.
- Full Vitest: 395 файлов, 3171 тест — pass.
- `tsc --noEmit` — pass.
- ESLint — 0 ошибок; 8 прежних предупреждений в файлах вне трека.
- `next build` — pass после разрешённого доступа к Google Fonts; первый запуск в sandbox завершился `ENOTFOUND fonts.googleapis.com`.
- `git diff --check` — pass.
- Headless Chrome/CDP на временном локальном Next.js route: 390 px без бронзового боя и 1280 px с бронзовым. На 390 px `innerWidth=scrollWidth=390`, ссылка целиком в пределах экрана (`right=374`), один якорь `results`, подписи 1/2/3–4/3–4. На 1280 px подписи пьедестала 1/2/3, в полном протоколе также 4. Скриншоты просмотрены; временный route удалён.
- Нативная загрузка `?spacer=1#results` в Chrome: `location.hash=#results`, целевой блок на 96 px от верхней границы, активный элемент `results`.

## Остаток приёмки

- Совмещение с 0053 проверить после последовательного merge: этот трек не меняет `bracket-view`, `bracket-seeding` и `entities/bracket`.
